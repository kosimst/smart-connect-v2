import styled from '@emotion/styled'

export const Main = styled.main`
  width: 100%;
  padding: 16px;
  padding-bottom: 80px;
  min-height: 100%;
  box-sizing: border-box;
  position: relative;
  overflow-x: hidden;
`
