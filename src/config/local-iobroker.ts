// Controls how many states will be fetched in one call at max
export const MAX_BULK_GET_SIZE = 10

// How often device types, rooms and traits will be fetched (ms)
export const ENUMS_REFETCH_INTERVAL = 10_000
