import HistoryConfig from '../types/history-config'

const climateSensor: HistoryConfig = {
  temperature: {},
  humidity: {},
  co2: {},
}

const historyConfigs = {
  'climate-sensor': climateSensor,
}

export default historyConfigs
